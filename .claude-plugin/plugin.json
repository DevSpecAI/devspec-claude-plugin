{
  "name": "devspec",
  "displayName": "DevSpec",
  "description": "Connect Claude Code to DevSpec: pull a batch of action items and work them, and drive sessions from the Agents page.",
  "version": "0.32.3",
  "author": {
    "name": "DevSpecAI",
    "url": "https://devspec.ai"
  },
  "homepage": "https://devspec.ai",
  "repository": "https://github.com/DevSpecAI/devspec-claude-plugin",
  "license": "MIT",
  "keywords": [
    "devspec",
    "autopilot",
    "automation",
    "mcp",
    "action-items",
    "remote-control"
  ],
  "userConfig": {
    "devspec_token": {
      "type": "string",
      "title": "DevSpec API token",
      "description": "Your DevSpec API token with read_write scope. Create one in DevSpec under You → Connections → Connect a tool (pick Read & write). It starts with 'dvs_' and is account-wide — reuse the same token everywhere.",
      "sensitive": true,
      "required": true
    },
    "devspec_mcp_url": {
      "type": "string",
      "title": "DevSpec server",
      "description": "Leave this as it is. It only changes if DevSpec ask you to point at a different server.",
      "default": "https://api.devspec.ai/api/mcp",
      "required": false
    },
    "connect_at_startup": {
      "type": "boolean",
      "title": "Make Claude Code available on DevSpec when it starts",
      "description": "When you start Claude Code in a folder linked to a DevSpec project, it appears on the Agents page straight away, ready for your team to send it work. It uses nothing while it waits. Turn this off to connect only when you run /devspec:devspec.remote.",
      "default": true,
      "required": false
    }
  },
  "mcpServers": {
    "devspec": {
      "type": "http",
      "url": "${user_config.devspec_mcp_url}",
      "headers": {
        "Authorization": "Bearer ${user_config.devspec_token}"
      }
    }
  },
  "commands": [
    "./commands"
  ],
  "experimental": {
    "monitors": [
      {
        "name": "devspec-remote",
        "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/devspec-remote-listen.mjs\"",
        "description": "DevSpec: messages sent to this agent (load the devspec-remote-command skill once per conversation, then answer each)"
      }
    ]
  }
}
